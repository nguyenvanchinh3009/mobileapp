<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HITCApp"
        tools:targetApi="31">
        <activity
            android:name=".DetaiActivity2"
            android:exported="false" />
        <!-- Đăng ký activity Đăng ký -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" /> <!-- LoginActivity2 là activity khởi động -->
        <activity
            android:name=".LoginActivity2"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Trang chủ sau khi đăng nhập -->
        <activity
            android:name=".HomeActivity2"
            android:exported="false" /> <!-- MainActivity không còn được dùng làm màn hình chính -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>